#!/bin/sh

set -e

if [ "$1" = "configure" ]
then
	if grep -qE "^(sql_)?connection.*sqlite.*" /etc/glance/glance-registry.conf
	then
		su -s /bin/sh -c 'glance-manage db_sync' glance
	fi
fi

#DEBHELPER#

